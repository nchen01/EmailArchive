# Copy to .env.local and fill in. The fixture mailbox UUID is printed by
# `python scripts/dev_seed.py`.
VITE_MAILBOX_ID=

# Optional: override the API base. Leave unset in dev — the Vite proxy
# forwards /api to http://localhost:8000.
# VITE_API_URL=
