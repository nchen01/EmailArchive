# Development convenience installer.
# pyproject.toml is the source of truth for all dependencies.
#
# Installs ekc_schemas in editable mode (changes to models.py are live)
# then the app with all dev/test/api/db/gmail deps in one shot:
-e packages/
-e .[dev]
#
# CI (no gmail deps, no editable schema): pip install -e .[test]
# Production (choose the extras the service needs): pip install .[api,db]
