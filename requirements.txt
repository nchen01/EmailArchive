# Development convenience installer.
# pyproject.toml is the source of truth for all dependencies.
#
# Install everything needed for local development and testing:
-e .[dev]
#
# Optional — only needed if you are actively editing packages/ekc_schemas/models.py
# and want changes to be live without reinstalling:
#   pip install -e packages/
#
# CI (no gmail deps): pip install -e .[test]
# Production (pick the service extras): pip install .[api,db]
